# include<stdio.h>
int main()
{
    int num1, num2, sum;
    printf("Enter two numbers:");
    scanf("%d %d", &num1, &num2);
    sum=num1+num2;
    if (sum %2== 0) {
        printf("even");
    }
    else{
        printf("Odd");
    }

     return 0;
}
